# Settings schema

The public settings snapshot contains `connections`, `logging`, `hotkeys`, `general`, and `windows`. `windows.floating` contains `opacity` (backend 10–100), `bg_color` (`#RRGGBB`), `clickthrough`, and optional `x`/`y`. General theme is `dark` or `light`.
